{
  "name": "document-distribution-service",
  "version": "0.1.0",
  "private": true,
  "description": "CargoDesk's first extracted microservice — owns EDI transmittal records and outbound Webhook delivery for shipment documents. Called only by the monolith (routes/document-distribution.js), never by the browser directly. Resolved dependencies (express) are hoisted from the repo root's node_modules — no separate install step.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node tests/webhook-configs.test.js && node tests/distribute.test.js"
  },
  "dependencies": {
    "express": "^4.18.3"
  }
}
